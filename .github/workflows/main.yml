name: Python CI

on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - shell: bash
      run: source ~/.bashrc
    - shell: bash
      run: botBuild
